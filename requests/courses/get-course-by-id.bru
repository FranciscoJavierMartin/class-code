meta {
  name: Get course by Id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/courses/:id
  body: none
  auth: inherit
}

params:path {
  id: {{courseId}}
}

vars:post-response {
  instructorId: res.body.course.instructorId
}

settings {
  encodeUrl: true
  timeout: 0
}
